name: test workflow

on:
  push:
    branches:
      - main
      -  develop

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: checkout code
        uses: actions/checkout@v3
      - name: Print Hello
        run: echo "Hello World"